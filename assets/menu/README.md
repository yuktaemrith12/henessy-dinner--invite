# Menu PDFs

Place the following files here for the menu links to work:

- `sushime.pdf` — SushiMe restaurant menu
- `grain-de-sel.pdf` — Grain de Sel restaurant menu

These are linked from `index.html` as local files so the page works correctly on GitHub Pages without depending on external restaurant websites.
